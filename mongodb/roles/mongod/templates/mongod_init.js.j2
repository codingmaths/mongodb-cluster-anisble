rs.initiate(
  {
    _id : "shardreplica01",
    members: [
    {% for host in groups['replication_servers'] %}
      { _id : {{loop.index}}, host: "{{host}}:{{mongod_port}}" },
    {% endfor %}
    ]
  }
)

printjson(rs.status());
